# 贡献指南

感谢你愿意参与 `netlens`。这个项目还很早期，任何形式的贡献都有价值——包括"某条诊断结论说得不对"这种反馈。

## 最有价值的贡献

按优先级排序：

1. **新的诊断规则。** 你在真实站点上发现了一个网络现象，它能解释某个 Web 性能问题，但 `netlens` 现在没提。这类贡献最受欢迎。
2. **现有规则的误报反馈。** 某条结论在你的场景下不成立。请附上 `--json` 输出，误报比漏报更伤害工具的可信度。
3. **协议层覆盖。** 例如 HTTP/3、QUIC、IPv6 双栈、连接复用的异常场景。
4. **文档与术语。** 如果你读某段说明卡住了，那段说明就有问题。

## 新增诊断规则的硬性要求

这是本项目最重要的一条约定。**一条规则不是一个 `if`，而是一个四元组**：

```text
网络现象  →  Web 性能后果  →  可执行动作  →  理论锚点
```

四个部分缺一不可：

| 部分 | 说明 | 反例 |
| --- | --- | --- |
| 网络现象 | 从 socket / 响应头里**真实观测到**的事实 | "感觉有点慢" |
| 性能后果 | 它如何影响用户可感知的加载表现 | "不够现代" |
| 可执行动作 | 使用者能直接做的一件事 | "优化网络" |
| 理论锚点 | 支撑这条判断的网络原理，一句话说清 | 无（不允许省略） |

**理论锚点不是装饰。** 它是这个项目同时作为"网络学习工具"存在的原因——每条结论都应该让使用者学到一点协议层的知识。提交规则时请贴出它在至少一个真实站点上的输出。

## 开发环境

- **Node.js**：见 `package.json` 的 `engines` 字段。
- 本项目**运行时不依赖任何第三方包**。这是刻意选择：诊断工具应该能在任何机器上零安装运行。
- 开发依赖保持最小，新增依赖需要在 PR 里说明理由。

## 本地开发

```bash
git clone <repo>
cd netlens
# 具体命令见 README 的"快速开始"；技术选型确定后补全
```

## 代码约定

- **文档与提交信息用中文**，代码标识符（变量、函数、类型、文件名）用英文。
- 注释解释"为什么"，不解释"是什么"。特别是网络行为，要写清"这个事件在协议层意味着什么"。
- 不要为了让数字好看而调整阈值。阈值改动必须附上真实站点的前后对比。
- 探测失败时给出失败在哪一层（DNS / TCP / TLS / HTTP），不要只抛一个笼统错误。

## 提交信息

使用中文，遵循以下格式：

```text
<类型>: <一句话说明>

<可选：为什么这么改，以及验证方式>
```

常用类型：`feat`（新功能）、`fix`（修复）、`docs`（文档）、`refactor`（重构）、`test`（测试）、`chore`（杂项）。

示例：

```text
feat: 增加 keep-alive 失效检测规则

服务端返回 Connection: close 时，热请求仍会新建连接，
原实现只对比耗时无法区分"复用失败"与"服务端慢"。
改用 req.reusedSocket 作为硬证据。
验证站点：example.com / chengyunlai.top
```

## 提交流程

1. 开一个 issue 描述你发现的网络现象（附站点、`--json` 输出、你的判断）。
2. Fork 并开分支，分支名形如 `feat/keepalive-detection`。
3. 在 `examples/<stage-id>-<slug>/` 下补一个可运行的示例（见 [examples/README.md](./examples/README.md)）。
4. 在真实站点上跑一遍，把真实输出贴进 PR 描述。
5. 提交 PR。

## 我们不接受什么

- 为了凑数量而添加的规则（没有理论锚点的规则会被直接退回）。
- 把 `netlens` 改造成性能评分工具——那是 Lighthouse 的领域。
- 引入重量级运行时依赖，破坏"零安装运行"这个特性。

## 行为准则

对事不对人。技术争论欢迎，人身攻击不接受。讨论请围绕"这个结论在什么条件下成立"。
