# 工作规则

本文件是协作者（人类与 AI）在本仓库工作的稳定规则，不记录某一个阶段的具体方案。

## 开始工作前，按顺序读

```text
README.md          → 项目在解决什么问题、当前阶段
AGENTS.md          → 本文件：怎么在这个仓库里工作
docs/context.md    → 稳定事实：术语、设计原则、约束、待定项
examples/README.md → 各阶段可运行示例的索引
docs/implementation/<stage>.md → 相关阶段的实现记录
```

## Example 归档

- 仓库**唯一的** canonical example 根目录是 `examples/`。所有阶段示例都放在这里，不要散落到临时目录或 `src/` 旁边。
- 每个阶段一个目录，命名 `examples/<stage-id>-<slug>/`。没有 issue key 时用 `stage-01`、`stage-02`。
- 每个阶段目录分成两部分：
  - `user_code/` —— 使用者视角的最短公共 API 组装路径，是**设计验收入口**
  - `core/` —— 对核心实现的解释与映射
- 先让 `user_code/` 能在真实场景跑通，再补 `core/`。如果 `user_code/` 写得不清爽，优先回查公开 API 和组装边界，不要靠增加教程文字遮掩。
- 阶段完成后更新 `examples/README.md` 的索引表。

## 实现规则

- 探测层不判断好坏，诊断层不碰网络，渲染层不参与判断。三层职责不要互相渗透。
- 新增或修改诊断规则时，四元组（现象 / 后果 / 动作 / 理论锚点）必须齐全。
- 提交前在至少一个真实站点上跑一遍，把真实输出作为证据贴进记录。
- 修改公开 API、目录结构或调用路径时，同步更新 `docs/context.md` 的受影响事实、example README 和 `examples/README.md`。

## 提交规则

- 提交信息用中文，代码标识符用英文。格式见 [CONTRIBUTING.md](./CONTRIBUTING.md)。
- 区分**实现提交**（代码可运行）与**记录提交**（导航、example、实现记录）。先用实现提交固化可运行代码，再用记录提交固化可发现性。
- 不要把历史 commit 里的行号当作当前断点——行号会漂移，用稳定符号（函数名、事件名）定位。

## 不要做

- 不要在未获明确授权时推送、创建远程仓库或发布 npm 包。
- 不要引入运行时第三方依赖。
- 不要把 `netlens` 改造成性能评分工具。
- 不要为了让输出好看而调阈值。
